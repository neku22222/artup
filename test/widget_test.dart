// Basic smoke test for ArtUp
// Note: full widget tests require a Supabase mock — this just verifies the app compiles.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    // Replace with real tests once you have a Supabase mock set up.
    expect(1 + 1, 2);
  });
}